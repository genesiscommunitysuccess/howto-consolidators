import './Home.css';
import { persistLayout } from '../../utils';

const Home = () => {
  return (
    <section className="home-page">
      Welcome to Home
    </section>
  );
};

export default Home;
